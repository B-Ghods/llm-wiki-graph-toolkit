# Copy this file to ".env" and fill in your real key.
# ask_graph.py loads ".env" automatically (via python-dotenv) and OPENAI_API_KEY
# is never sent anywhere except OpenAI's API.

OPENAI_API_KEY=sk-...

# Optional overrides (defaults shown):
# OPENAI_MODEL=gpt-4o-mini
